# V1.5 Stability & Integration Tasks

This checklist addresses the "Partially Implemented" findings to ensure the AtlanticProxy client is stable, data-accurate, and production-ready (mock-mode).

## 1. 📊 Real-Time Statistics Integration (Priority: High)
**Goal**: Remove all hardcoded mocks from the dashboard and display real usage data from the backend.
- [x] **Backend**: Verify `/api/billing/usage` returns the global `UsageTracker` stats (Data Transferred, Request Count).
- [x] **Frontend**: Update `StatisticsPage.tsx` to fetch from `/api/billing/usage` instead of using static placeholders.
- [x] **Frontend**: Connect the "Avg. Latency" card to the `ProxyStatus.Latency` field (averaged or realtime).

## 2. 🛡️ TUN Interface "Safety Mode" (Priority: Medium)
**Goal**: Ensure the TUN interface doesn't act as a "black hole" for traffic if full VPN routing isn't ready.
- [x] **Backend**: Update `tun_darwin.go` to explicitly **reject** or **pass-through** packets it cannot handle, rather than just logging them (which causes timeouts).
- [x] **Backend**: Add a "Traffic Monitor" mode that only counts bytes without diverting traffic, ensuring stability when "VPN Mode" is off.

## 3. 💳 Billing Flow Verification (Test Mode) (Priority: Medium)
**Goal**: Verify the full subscription lifecycle using the placeholder keys.
- [x] **Backend**: Ensure `billing.go` accepts the "test_token" from the frontend and correctly upgrades the user's plan in the internal memory/DB.
- [x] **Frontend**: Verify the "Upgrade Plan" -> "Payment Success" -> "Dashboard Update" flow updates the UI immediately without a refresh.

## 4. 🎭 Security Score Realism (Priority: Low)
**Goal**: Make the "Anonymity Score" reflect actual leak tests.
- [x] **Backend**: Wire the `validator.LeakDetector` results (DNS/IP leaks) directly to the `SecurityStatus` response.
- [x] **Dashboard**: Update the "Privacy Shield" card to show specific warnings (e.g., "DNS Leak: Cloudflare detected") based on the real validator output.

---

### Definition of Done (Stability)
After completing these tasks:
1.  **No "Fake" Data**: Every number on the dashboard comes from the Go backend.
2.  **No Network Black Holes**: Enabling the "Protection" switch won't kill internet connectivity (due to proper TUN handling).
3.  **Testable Commerce**: You can demo the full purchase flow.

### ✅ Verification Log
- **Date**: 2026-01-05
- **Status**: PASSED
- **Tests**: `go test ./...` passed (fixed initialization injection in `service.go`).
- **Validation**:
    - Dashboard stats are wired to backend.
    - TUN interface has "pass-through" safety mode.
    - Billing flow handles test tokens.
    - Security score reflects real leak detector output.

**Ready for V1.6 Deployment Phase.**
